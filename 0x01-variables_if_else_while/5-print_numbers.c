#include <stdio.h>
/**
 * main - A main fuction thay delcrea data type
 * loop - a loop fuction
 * Return: this return zero
 */
int main(void)
{
	char A;

	A = 0;
	while
		(A <= 9) {
			putchar(A);
			A++;
		}
	putchar('\n');
	return (0);
}
