#include "main.h"

/**
 * add - a function thay check for lower and uppercase char
 * @a: parameter
 * @b: parameter
 * Return: return sum
 */

int add(int a, int b)
{
	int sum;
	sum = a + b;
	return (sum);
}
