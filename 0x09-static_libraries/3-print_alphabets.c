#include <stdio.h>
/**
 * main - A main fuction thay delcrea char data type
 * loop - a loop fuction
 * Return: this return zero
 */
int main(void)
{
	char A;

	char B;

	A = 'a';
	B = 'A';
	while
		(A <= 'z') {
			putchar(A);
			A++;
		}
	while
		(B <= 'Z') {
			putchar(B);
			B++;
		}
	putchar('\n');
	return (0);
}
