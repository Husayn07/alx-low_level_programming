#include <stdio.h>
/**
 * main - A main fuction thay delcrea data type
 * loop - a loop fuction
 * Return: this return zero
 */
int main(void)
{
	int A;

	A = 0;
	while
			(A <= 9) {			printf("%d", A);
			A++;
		}
	printf("\n");
	return (0);
}
