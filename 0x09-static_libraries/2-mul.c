#include "main.h"

/**
 * mul - a function thay check for lower and uppercase char
 * @a: parameter
 * @b: parameter
 * Return: return sum
 */

int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
