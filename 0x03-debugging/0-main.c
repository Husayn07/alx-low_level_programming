#include "main.h"

/**
 * * main - tests function that prints if integer is positive or negative
 * * Return: 0
 * */

int main(void)
{
	        int i;
	      
		i = 98;
		int r = positive_or_negative(i);
		printf("%d is %d\n", i, r);
 return (0);
}
